{
  "framework": null,
  "installCommand": "cd backend && npm ci && cd ../frontend && npm ci",
  "buildCommand": "cd backend && npm run build && cd .. && npx @vercel/ncc build api/index.ts -o api --target node18 && rm -f api/index.ts && cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
