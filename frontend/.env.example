# URL base del backend (solo necesario si frontend y backend están en dominios distintos).
# Si despliegas todo en un solo proyecto Vercel desde la raíz del repo, déjalo vacío (se usa /api en el mismo dominio).
# Si el backend está en otro proyecto Vercel, pon la URL del backend, por ejemplo:
# VITE_API_URL=https://tu-backend.vercel.app

VITE_API_URL=
