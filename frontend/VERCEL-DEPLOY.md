# Deploy en Vercel

Este repo es solo **frontend** (Vite + React). El backend está en otro entorno.

- **Root Directory** en Vercel: **`frontend`**
- **Framework Preset**: Vite
- Variable **VITE_API_URL**: URL de tu backend (sin barra final)
